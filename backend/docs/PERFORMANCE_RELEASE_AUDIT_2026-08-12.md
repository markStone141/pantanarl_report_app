# Performance 公開前総合監査

実施日: 2026-08-12

## 対象

- ブランチ: `refactor/performance-today-details`
- 監査開始時HEAD: `b80fd47`
- 比較基準: `origin/main`
- 対象: `performance` 初期8工程、次期4工程、関連UI変更、メール日次集計修正を含む13コミット

## 公開判定基準

- 既存URL、権限、UN/WV分岐、主要context契約を維持する
- 通常実績、補正実績、WVキャンセルの月・路程集計を維持する
- メールの当日実績には通常の日次報告だけを表示し、補正は月・路程累計だけに含める
- メンバー数・transactions数に比例するN+1を再発させない
- AJAXの必須引数、編集可否、戻り先、遅延取得の応答契約を維持する
- 全体テスト、Django check、migration check、BOM、EOF/差分、JavaScript lintが成功する

## 結果

| 観点 | 結果 | 根拠 |
|---|---|---|
| 全体回帰 | 成功 | Django全379テスト成功 |
| Django設定 | 成功 | `manage.py check` で問題なし |
| migration | 成功 | `makemigrations --check --dry-run` で変更なし |
| 文字コード | 成功 | `check_no_bom.py` 成功 |
| 差分形式 | 成功 | `git diff --check origin/main...HEAD` 成功 |
| JavaScript | 成功 | ESLint 8.57.1でエラーなし |
| メール日次/累計 | 成功 | UN/WVとも日次は通常報告のみ、累計は補正込みのテスト成功 |
| 補正のみの日 | 成功 | 当日活動として扱わず、累計へだけ反映するテスト成功 |
| DBアクセス | 成功 | transactionsの順序付きPrefetchとクエリ数テスト成功 |
| AJAX/権限 | 成功 | 管理者・本人・閲覧専用、日別/一覧、候補APIの回帰テスト成功 |
| テスト監査 | 問題なし | 責務移動では期待値を維持し、業務仕様変更部分だけ期待値を更新 |

## 検出した環境事項

- `.venv` の `python3` シンボリックリンクが欠けていたため、システムPythonへ `.venv` のsite-packagesを指定して全テストを実行した。依存パッケージ自体は存在し、アプリの失敗ではない。
- `node_modules` とlockfileがなかったため、固定バージョンのESLint 8.57.1を一時キャッシュで実行した。リポジトリファイルは変更していない。

## 残リスクと公開判断

- 自動テストと静的監査の範囲では公開を止める問題はない。
- 実ブラウザでの公開環境確認はPUSH・デプロイ後に行う。
- 現在の実行環境ではGitHubへの外部書き込みが拒否されるため、PUSHは別の許可済み実行経路が必要。

判定: **PUSH可能。公開環境の主要導線確認を条件に公開完了とする。**
